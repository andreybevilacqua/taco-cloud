spring:
  application:
    name: Taco-Hystrix-Dashboard

server:
  port: 7979

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

management:
  endpoints:
    health:
      show-details: always
    web:
      exposure:
        include: "*"